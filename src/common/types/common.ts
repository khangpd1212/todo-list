export enum TypeAction {
  INIT = "init",
  ADD = "add",
  UPDATE = "update",
  DELETE = "delete",
}

export enum StatusTask {
  OPEN = "open",
  TODO = "todo",
  INPROGRESS = "in-progress",
  DONE = "done",
}
